
# ▗▄▄▄  ▗▄▄▄▖▗▄▄▄▖▗▖  ▗▖     ▗▄▄▖▗▖ ▗▖ ▗▄▖▗▖  ▗▖     ▗▄▄▖ ▗▄▖ ▗▖  ▗▖▗▄▄▄▖▗▄▄▄▖ ▗▄▄▖
# ▐▌  █   █  ▐▌    ▝▚▞▘     ▐▌   ▐▌ ▐▌▐▌ ▐▌▝▚▞▘     ▐▌   ▐▌ ▐▌▐▛▚▖▐▌▐▌     █  ▐▌
# ▐▌  █   █  ▐▛▀▀▘  ▐▌       ▝▀▚▖▐▌ ▐▌▐▛▀▜▌ ▐▌      ▐▌   ▐▌ ▐▌▐▌ ▝▜▌▐▛▀▀▘  █  ▐▌▝▜▌
# ▐▙▄▄▀ ▗▄█▄▖▐▙▄▄▖▗▞▘▝▚▖    ▗▄▄▞▘▐▙█▟▌▐▌ ▐▌ ▐▌      ▝▚▄▄▖▝▚▄▞▘▐▌  ▐▌▐▌   ▗▄█▄▖▝▚▄▞▘

# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Screen Config
include ~/.config/sway/conf.d/10-Display.conf

# Auto exec
include ~/.config/sway/conf.d/20-AutoStart.conf

# App Window Config
include ~/.config/sway/conf.d/30-AppWindow.conf

# KeyBind Config
include ~/.config/sway/conf.d/40-KeyBind.conf

# Window Border
include ~/.config/sway/conf.d/50-WindowBorder.conf

# Workspaces out put for monitors
include ~/.config/sway/conf.d/60-Workspaces.conf

include '$(/usr/libexec/sway/layered-include "/usr/share/sway/config.d/*.conf" "/etc/sway/config.d/*.conf" "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/*.conf")'

#----------------- Switch language --------------------

# Comment out the generic input block if it still exists (using the # symbol):

 input * {
     xkb_layout "us,ara"
     xkb_options "grp:alt_shift_toggle"
 }

# Also, comment out the last workaround we tried:
exec setxkbmap -layout "us,ara" -option "grp:alt_shift_toggle"

# --- KEEP THE XKB PATH FIX (This is vital) ---
exec export XKB_CONFIG_ROOT=/usr/share/X11/xkb

# --- NEW: TARGETED INPUT BLOCK ---
# This block targets your specific keyboard device by its identifier.
input "13357:58566:MILE_EPOMAKER_Split65_Keyboard" {
    xkb_layout "us,ara"
    xkb_options "grp:alt_shift_toggle"
}

#-----------------------------------------------------

floating_modifier $mod

# Your preferred terminal emulator
set $term alacritty

# Load Pywal colors
#include ~/.cache/wal/colors-sway
#exec_always ~/.config/wal/wal-set

# # -----------------------------------------------------------------------------
# # GRUVBOX COLORSCHEME DEFINITIONS
# # -----------------------------------------------------------------------------
# # Gruvbox Dark Medium Palette
# set $gruv_bg        #282828
# set $gruv_fg        #ebdbb2
# set $gruv_red       #cc241d
# set $gruv_green     #98971a
# set $gruv_yellow    #d79921
# set $gruv_blue      #458588
# set $gruv_purple    #b16286
# set $gruv_aqua      #689d6a
# set $gruv_orange    #d65d0e
# set $gruv_bg_dark   #1d2021
# set $gruv_bg_light  #3c3836
# set $gruv_gray      #a89984

# # -----------------------------------------------------------------------------
# # WINDOW BORDER CONFIGURATION
# # -----------------------------------------------------------------------------
# # Set the gaps between windows
# gaps inner 15

# # Set the gaps around the edge of the screen
# gaps outer 5

# # Set the border style and thickness
# default_border pixel 4
# default_floating_border pixel 4

# # Pywall colors sync
# client.focused           $color7 $color7 $color7 $color7
# client.focused_inactive  $color1 $color1 $color1 $color1
# client.unfocused         $color1 $color1 $color1 $color1
# client.urgent            #7A8862 #7A8862 #7A8862 #7A8862 #7A8862
# client.placeholder       #7A8862 #B8A78F #B8A78F #7A8862 #B8A78F

# --------------------------WORKSPACE------------------------------


# # Bind workspace 1 to a specific output and make it persistent
# workspace 1 output DP-1

# # Bind workspace 2 to a specific output and make it persistent
# workspace 2 output DP-1

# # Bind workspace 3 to a specific output and make it persistent
# workspace 3 output DP-1

# # Bind workspace 4 to a specific output and make it persistent
# workspace 4 output DP-1

# # Bind workspace 5 to a specific output and make it persistent
# workspace 5 output DP-1

# # Bind workspace 6 to a specific output and make it persistent
# workspace 6 output DP-3

# # Bind workspace 7 to a specific output and make it persistent
# workspace 7 output DP-3

# # Bind workspace 7 to a specific output and make it persistent
# workspace 8 output DP-3

# # Bind workspace 8 to a specific output and make it persistent
# workspace 9 output DP-3

# ---------------------------------------------------------------

#------------ Gnome Schema config ---------------- 

#set $gnome-schema org.gnome.desktop.interface

#exec_always {
#    gsettings set $gnome-schema gtk-theme 'Orchis-Grey-Dark'
#    gsettings set $gnome-schema icon-theme 'kora'
#    gsettings set $gnome-schema cursor-theme 'Bibata-Original-Classic'
#    gsettings set $gnome-schema font-name 'GoMono Nerd Font Mono 12'
#}

#For Cursor
#seat seat0 xcursor_theme Bibata-Original-Classic 24
#set $icon_theme
#exec_always export XDG_ICON_THEME=$icon_theme


# This forces the icon theme to load before Wofi
#exec gsettings set org.gnome.desktop.interface icon-theme 'kora'

#--------------------------------------------------
# FORCES SWAY TO FIND XKB CONFIG FILES
#exec export XKB_CONFIG_ROOT=/usr/share/X11/xkb
### Key bindings
#
# Basics:
#
    # Start a terminal
#    bindsym $mod+q exec $term

    # Kill focused window
#    bindsym $mod+Shift+q kill

    # Start your launcher
#    bindsym $mod+d exec fuzzel show menu
   
    # Start file manager by yazi and kitty term
    # Start Thunar
#    bindsym $mod+Shift+f exec thunar
    # kitty --class yazifile -e bash -lc "/home/diex/yazifile.sh"

   
    # Start slurp screenshot tool
    #bindsym $mod+Shift+s exec grim -g "$(slurp)" /home/diex/Pictures/screenshot-$(date +%s).png
#    bindsym $mod+Shift+s exec bash -c 'FILE="/home/diex/Pictures/screenshot-$(date +%s).png"; grim -g "$(slurp)" "$FILE" && wl-copy < "$FILE"'

    # Start emacs
#    bindsym $mod+Shift+e exec emacs

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
#    floating_modifier $mod normal

    # Reload the configuration file
#    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    #bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Moving around:
#
    # Move your focus around
#    bindsym $mod+$left focus left
#    bindsym $mod+$down focus down
#    bindsym $mod+$up focus up
#    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
#    bindsym $mod+Left focus left
#    bindsym $mod+Down focus down
#    bindsym $mod+Up focus up
#    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
#    bindsym $mod+Shift+$left move left
#    bindsym $mod+Shift+$down move down
#    bindsym $mod+Shift+$up move up
#    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
#    bindsym $mod+Shift+Left move left
#    bindsym $mod+Shift+Down move down
#    bindsym $mod+Shift+Up move up
#    bindsym $mod+Shift+Right move right
#
# Workspaces:

#    
    # Switch to workspace
#    bindsym $mod+1 workspace number 1
#    bindsym $mod+2 workspace number 2
#    bindsym $mod+3 workspace number 3
#    bindsym $mod+4 workspace number 4
#    bindsym $mod+5 workspace number 5
#    bindsym $mod+6 workspace number 6
#    bindsym $mod+7 workspace number 7
#    bindsym $mod+8 workspace number 8
#    bindsym $mod+9 workspace number 9
#    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
#    bindsym $mod+Shift+1 move container to workspace number 1
#    bindsym $mod+Shift+2 move container to workspace number 2
#    bindsym $mod+Shift+3 move container to workspace number 3
#    bindsym $mod+Shift+4 move container to workspace number 4
#    bindsym $mod+Shift+5 move container to workspace number 5
#    bindsym $mod+Shift+6 move container to workspace number 6
#    bindsym $mod+Shift+7 move container to workspace number 7
#    bindsym $mod+Shift+8 move container to workspace number 8
#    bindsym $mod+Shift+9 move container to workspace number 9
#    bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
#    bindsym $mod+b exec brave-browser
#    bindsym $mod+v splitv

    # Switch the current container between different layout styles
#    bindsym $mod+s layout stacking
#    bindsym $mod+w layout tabbed
#    bindsym $mod+e layout toggle split

#     # Make the current focus fullscreen
#     bindsym $mod+f fullscreen

#     # Toggle the current focus between tiling and floating mode
#     bindsym $mod+Shift+space floating toggle

#     # Swap focus between the tiling area and the floating area
#     bindsym $mod+space focus mode_toggle

#     # Move focus to the parent container
#     bindsym $mod+a focus parent
# #
# # Scratchpad:
# #
#     # Sway has a "scratchpad", which is a bag of holding for windows.
#     # You can send windows there and get them back later.

#     # Move the currently focused window to the scratchpad
#     bindsym $mod+Shift+minus move scratchpad

#     # Show the next scratchpad window or hide the focused scratchpad window.
#     # If there are multiple scratchpad windows, this command cycles through them.
#     bindsym $mod+minus scratchpad show
# #
# # Resizing containers:
# #
# mode "resize" {
#     # left will shrink the containers width
#     # right will grow the containers width
#     # up will shrink the containers height
#     # down will grow the containers height
#     bindsym $left resize shrink width 10px
#     bindsym $down resize grow height 10px
#     bindsym $up resize shrink height 10px
#     bindsym $right resize grow width 10px

#     # Ditto, with arrow keys
#     bindsym Left resize shrink width 10px
#     bindsym Down resize grow height 10px
#     bindsym Up resize shrink height 10px
#     bindsym Right resize grow width 10px

#     # Return to default mode
#     bindsym Return mode "default"
#     bindsym Escape mode "default"
# }
# bindsym $mod+r mode "resize"
#include '$(/usr/libexec/sway/layered-include "/usr/share/sway/config.d/*.conf" "/etc/sway/config.d/*.conf" "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/*.conf")'

# ======================
# Border Control Mode
# ======================

# # Enter border mode
# bindsym $mod+Shift+b mode "borders"

# mode "borders" {
#     # Toggle between border styles
#     bindsym t border toggle

#     # Set specific border types
#     bindsym n border none
#     bindsym s border normal
#     bindsym 1 border pixel 1
#     bindsym 2 border pixel 2
#     bindsym 3 border pixel 3

#     # Exit mode
#     bindsym Escape mode "default"
#     bindsym Return mode "default"
# }

# #audio control
# 	bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
# 	bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
# 	bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
#     bindsym XF86AudioPlay exec mpc toggle
#     bindsym XF86AudioNext exec mpc next
#     bindsym XF86AudioPrev exec mpc prev
#     bindsym XF86AudioStop exec mpc stop
